## DATA TYPE

##1.numberic
x = 1
class(x)
 
##2.charecter
name = "nut"
class(name)

##3.logical boole
real = TRUE
class(real)

##4.factor
box = c("apple" ,"apple" ,"cat", "warm")
class(box)

box = factor(box)
class(box)

##5.date/time
date = Sys.time()
class(date)
